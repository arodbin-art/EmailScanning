# API environment
DATABASE_URL=postgresql://user:pass@localhost:5432/signal_engine
ADMIN_TOKEN=replace-with-strong-token
ADMIN_ALLOWED_ORIGINS=http://localhost:5175
AI_ENABLED=true
PORT=4000
# When deploying as a single container (serving UI from API), point at the built UI directory.
UI_DIST_PATH=
